# MEC Automation Tool — Python dependencies
# Install all at once: pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Excel read/write (trial balances, variance reports, JE templates)
openpyxl>=3.1.0

# Word document generation (close reports)
python-docx>=1.1.0

# YAML support (legacy config files)
pyyaml>=6.0

# Anthropic Claude API (real-time + batch commentary)
anthropic>=0.40.0

# Progress bars in the CLI
tqdm>=4.66.0

# Optional: load ANTHROPIC_API_KEY from a .env file automatically
# Remove the '#' below to enable .env file support
# python-dotenv>=1.0.0
